<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Paninda.Views.NotificationPage"
             BackgroundColor="#F2EFEF">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <!-- HEADER with back button -->
            <HorizontalStackLayout Spacing="10">
                <Border BackgroundColor="#C084FC"
                        WidthRequest="40"
                        HeightRequest="40"
                        StrokeThickness="0"
                        Padding="0">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="20" />
                    </Border.StrokeShape>
                    <Label Text="←" HorizontalOptions="Center" VerticalOptions="Center" TextColor="White">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnBackClicked" />
                        </Label.GestureRecognizers>
                    </Label>
                </Border>
                <Label Text="NOTIFICATION"
                       FontSize="24"
                       FontAttributes="Bold"
                       TextColor="#E5A823"
                       VerticalOptions="Center"/>
            </HorizontalStackLayout>

            <!-- DYNAMIC NOTIFICATION CARD -->
            <Border BackgroundColor="#E6E1E1" StrokeThickness="0" Padding="20">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="25" />
                </Border.StrokeShape>
                <CollectionView x:Name="NotificationsList" ItemsLayout="VerticalList">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <VerticalStackLayout Spacing="2" Margin="0,10">
                                <Label Text="{Binding Title}"
                                       TextColor="#D9534F"
                                       FontAttributes="Bold"/>
                                <Label Text="{Binding Message}"
                                       TextColor="Black"/>
                            </VerticalStackLayout>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </Border>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>